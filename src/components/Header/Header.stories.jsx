import React from 'react';

import Header from './Header';

/**
 * This story has no variations performed on the component itself.
 */
export default {
    title: 'Header',
    component: Header,
};

const Template = () => <Header />;

export const NoVariations = Template.bind({});
